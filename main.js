function getParagraph1(){
    var inputs = [];
    for(var i = 1 ; i <=6 ; i++){
        inputs.push(document.getElementsById("para1_input_box_" + i).value);
    }
    inputs.join(".");
    document.getElementsById("showParagraph1").innerHTML = inputs.join(".")

    var inputs = [];
    for(var i = 2 ; i <=6 ; i++){
        inputs.push(document.getElementsById("para1_input_box_" + i).value);
    }
    inputs.join(".");
    document.getElementsById("showParagraph1").innerHTML = inputs.join(".")

    var inputs = [];
    for(var i = 3 ; i <=6 ; i++){
        inputs.push(document.getElementsById("para1_input_box_" + i).value);
    }
    inputs.join(".");
    document.getElementsById("showParagraph1").innerHTML = inputs.join(".")

    var inputs = [];
    for(var i = 4 ; i <=6 ; i++){
        inputs.push(document.getElementsById("para1_input_box_" + i).value);
    }
    inputs.join(".");
    document.getElementsById("showParagraph1").innerHTML = inputs.join(".")

    var inputs = [];
    for(var i = 5 ; i <=6 ; i++){
        inputs.push(document.getElementsById("para1_input_box_" + i).value);
    }
    inputs.join(".");
    document.getElementsById("showParagraph1").innerHTML = inputs.join(".")

    var inputs = [];
    for(var i = 6 ; i <=6 ; i++){
        inputs.push(document.getElementsById("para1_input_box_" + i).value);
    }
    inputs.join(".");
    document.getElementsById("showParagraph1").innerHTML = inputs.join(".")
}








function getParagraph2(){
    var inputs = [];
    for(var i = 1 ; i <=6 ; i++){
        inputs.push(document.getElementsById("para2_input_box_" + i).value);
    }
    inputs.join(".");
    document.getElementsById("showParagraph2").innerHTML = inputs.join(".")

    var inputs = [];
    for(var i = 2 ; i <=6 ; i++){
        inputs.push(document.getElementsById("para2_input_box_" + i).value);
    }
    inputs.join(".");
    document.getElementsById("showParagraph2").innerHTML = inputs.join(".")

    var inputs = [];
    for(var i = 3 ; i <=6 ; i++){
        inputs.push(document.getElementsById("para2_input_box_" + i).value);
    }
    inputs.join(".");
    document.getElementsById("showParagraph2").innerHTML = inputs.join(".")

    var inputs = [];
    for(var i = 4 ; i <=6 ; i++){
        inputs.push(document.getElementsById("para2_input_box_" + i).value);
    }
    inputs.join(".");
    document.getElementsById("showParagraph2").innerHTML = inputs.join(".")

    var inputs = [];
    for(var i = 5 ; i <=6 ; i++){
        inputs.push(document.getElementsById("para2_input_box_" + i).value);
    }
    inputs.join(".");
    document.getElementsById("showParagraph2").innerHTML = inputs.join(".")
    
    var inputs = [];
    for(var i = 6 ; i <=6 ; i++){
        inputs.push(document.getElementsById("para2_input_box_" + i).value);
    }
    inputs.join(".");
    document.getElementsById("showParagraph2").innerHTML = inputs.join(".")
}